{
  "title": "<%= appname %>",
  "description": "",
  "keywords": "",<% if (sitetype === 'static') { %>
  "url": "http://www.example.com",<% } %>
  "locales": ["en"],<% if (routing) { %>
  "autoRouting": true,<% } %>
  "sourceDir": "app",
  "buildDir": "public",
  "configDir": "config",
  "viewsDir": "app/views",
  "webFont": {
    "typekit": {
      "id": null
    }
  }<% if (sitetype === 'static') { %>,
  "collections": {
  }<% } %>
}
